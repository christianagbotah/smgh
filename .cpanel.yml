---
deployment:
  tasks:
    - export DEPLOYPATH=/home/lightwor/smgh-web
    - /bin/mkdir -p $DEPLOYPATH/.next
    - /bin/cp -R src $DEPLOYPATH/
    - /bin/cp -R prisma $DEPLOYPATH/
    - /bin/cp -R public $DEPLOYPATH/
    - /bin/cp -R scripts $DEPLOYPATH/
    - /bin/cp -R .next/standalone $DEPLOYPATH/.next/standalone
    - /bin/cp -R .next/static $DEPLOYPATH/.next/static
    - /bin/cp package.json $DEPLOYPATH/
    - /bin/cp next.config.ts $DEPLOYPATH/
    - /bin/cp tsconfig.json $DEPLOYPATH/
    - /bin/cp app.js $DEPLOYPATH/
    - /bin/cp server.js $DEPLOYPATH/
    - /bin/cp .htaccess $DEPLOYPATH/
    - cd $DEPLOYPATH && npm install --production
    - cd $DEPLOYPATH && npx prisma generate
